<IfModule mod_rewrite.c>
RewriteEngine On

# Do NOT redirect real files
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# React fallback
RewriteRule . /index.html [L]
</IfModule>
